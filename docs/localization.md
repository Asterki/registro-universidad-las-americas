# Localization and i18n

The client contains localization resources (see `client/src/locales`).

Guidance

- Keep translation keys descriptive and stable.
- Provide both content and date/number formatting rules per locale.
- Document new keys and add fallback strings.
